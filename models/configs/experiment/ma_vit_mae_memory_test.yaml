# @package _global_

defaults:
  - override /data: mri
  - override /model: vit_mae
  - override /callbacks: mae
  - override /trainer: gpu
  - override /logger: wandb

tags: ["ma", "mri", "vit", "mae", "memory-test"]

seed: 12345

trainer:
  #min_epochs: 1  # prevents early stopping
  max_epochs: 1
  #accumulate_grad_batches: 10
  #accelerator: auto

model:
  optimizer:
    #weight_decay: 0.006853864581783747
    lr: 0.0002 # 0.0002 for overfitting 3 batches
  image_size: 3072
  patch_size: 48
  #hidden_size: 4080 # 2040 # 768 # 576
  #intermediate_size: 8192 # 16384 # 8192 # 4096 # 3072
  #mask_ratio: 0.75

data:
  #total_data_size: 1000
  #df_name: clean_df_slim_1000
  batch_size: 1
  image_size: 3072 # None
  square: True # False
  # pad_to_multiple_of: 48 # = patch_size
  output_channels: 1
  fix_inverted: True
  #batch_binning: strict
  cache: False
  num_workers: 2
  persistent_workers: True

logger:
  wandb:
    tags: ${tags}
