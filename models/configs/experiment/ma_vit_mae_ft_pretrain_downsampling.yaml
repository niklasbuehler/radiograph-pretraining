# @package _global_

defaults:
  - ma_vit_mae_ft_pretrain
  
trainer:
  accumulate_grad_batches: 16 # keep the same effective batch_size

data:
  downsampling: 0.5
  batch_size: 64 # double batch size
  num_workers: 16